'use client';

import ARIntegratedCollaboration from '../../components/ARIntegratedCollaboration';

export default function ARIntegratedCollaborationPage() {
  // Render the AR integrated collaboration component
  const arIntegratedCollaboration = ARIntegratedCollaboration();
  return arIntegratedCollaboration;
}
